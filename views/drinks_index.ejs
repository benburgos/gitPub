<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>gitPub Drink Menu</title>
</head>
<body>
    <h1>Welcome to the gitPub drink menu!</h1>
    <ul>
        Drink List
        <% for( let drink in allDrinks) { %>
            <% let drinkName = 
                allDrinks[drink].name.toLowerCase()
                .split(' ')
                .map((string) => string.charAt(0).toUpperCase() + string.substring(1))
                .join(' ') %>
            <li>
                <a href='drinks/<%= drink %>'><%= drinkName %></a>
            </li>
        <% } %>
    </ul>
    <style>
        body {
            color: blanchedalmond;
            background-color: steelblue;
        }
    </style>
</body>
</html>